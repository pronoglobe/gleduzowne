import 'package:flutter/material.dart';

class Compositions extends StatelessWidget {
  const Compositions({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      color: Colors.purple,
      child: const Center(child: Text('Compositions')),
    );
  }
}